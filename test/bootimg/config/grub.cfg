set default='0'
if [ x"$default" = xsaved ]; then
    load_env
    set default="$saved_entry"
fi

set timeout=3

if [ 0"$initialized" = "0" ]; then
    #set gfxmode=auto
    #set gfxpayload=keep
    #vbetest
    #terminal_output gfxterm
    set initialized=1
fi

